server:
  port: 9191

# 小傅哥扳手通用组件配置
xfg:
  wrench:
    config:
      system: test-system
      register:
        # redis host
        host: 192.168.1.109
        # redis port
        port: 16379

# 日志
logging:
  level:
    root: info
  config: classpath:logback-spring.xml