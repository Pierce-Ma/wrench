server:
  port: 9191

# 小傅哥扳手通用组件配置
xfg:
  wrench:
    config:
      system: test-system
      register:
        # redis host
        host: 127.0.0.1
        # redis port
        port: 6379

# 日志
logging:
  level:
    root: info
  config: classpath:logback-spring.xml
